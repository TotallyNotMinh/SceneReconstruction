# Scene Reconstruction & Digital Twin Pipeline Dependencies
# numpy is pinned below 2.0 — packages compiled against numpy 1.x (torchvision,
# open3d, sklearn, etc.) raise ABI errors if numpy is upgraded to 2.x.
numpy>=1.24.0,<2.0
# opencv-python-headless works on both desktop and headless (Kaggle/server) environments.
# Do NOT install both opencv-python and opencv-python-headless — they conflict.
opencv-python-headless>=4.8.0
torch>=2.0.0
torchvision
trimesh>=4.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
open3d>=0.17.0
ultralytics>=8.0.0
transformers>=4.30.0
pillow>=10.0.0
tqdm>=4.65.0
# mobile-sam PyPI package only supports Python <=3.11; install from git source instead.
# Kaggle (Python 3.12) and other modern environments must use the git version.
mobile-sam @ git+https://github.com/ChaoningZhang/MobileSAM.git
# depth-anything-3 has no PyPI release — install from source:
# git clone https://github.com/ByteDance-Seed/Depth-Anything-3.git && pip install -e .
# OR: pip install git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
# See README Step 1.3 for full instructions.
